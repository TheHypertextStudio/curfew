{
  "name": "curfew-monorepo",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "dev": "turbo run dev",
    "deploy": "turbo run deploy",
    "test": "pnpm --filter @curfew/license-worker test",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2"
  }
}
