# Curfew v0.1.0 release evidence

Release status: **blocked**

This file is append-only release evidence. Mark a release TODO complete only
after the corresponding row contains an artifact hash and a passing outcome.

## Candidate identity

| Field | Value |
|---|---|
| Version | `0.1.0` |
| Artifact SHA-256 | Pending signed DMG |
| Validation Mac | Pending sacrificial Mac |
| macOS | Pending |
| Validation date | Pending |
| Operator | Pending |

## Local automation

| Date | Command | Outcome |
|---|---|---|
| 2026-07-31 | `xcodebuild -resolvePackageDependencies -project Curfew.xcodeproj` | Pass; Sparkle resolved exactly to `2.9.4`. |
| 2026-07-31 | Targeted ownership, feature-flag, helper, daemon-state, lifecycle, heartbeat-cadence, override, updater, and daemon signing-requirement tests | Pass, including red/green proof for the 30-second heartbeat cadence and release-identity signing requirement. |
| 2026-07-31 | Ownership suite, 10 consecutive runs | Pass; no concurrency leakage observed. |
| 2026-07-31 | Three consecutive full `CurfewTests` runs | Pass before the final signing-requirement refactor; final three-run repetition recorded below. |
| 2026-07-31 | `swiftformat Curfew CurfewTests CurfewUITests --lint` | Pass; no formatting violations. |
| 2026-07-31 | `swiftlint lint --strict` | Pass; zero violations in 156 files. |
| 2026-07-31 | `just check` | Pass; format lint, strict lint, isolated unit target, and Debug build all succeeded. |
| 2026-07-31 | `xcodebuild build -project Curfew.xcodeproj -scheme CurfewWidget -configuration Debug -destination 'platform=macOS,arch=arm64'` | Pass; package-backed Widget target compiles through its generated scheme. |
| 2026-07-31 | `xcodebuild build -project Curfew.xcodeproj -scheme Curfew -configuration Debug -destination 'platform=macOS'` | Pass; app contains Sparkle, Widget, and individually signed helper binaries. |
| 2026-07-31 | `xcodebuild build -project Curfew.xcodeproj -scheme Curfew -configuration Release -destination 'platform=macOS'` | Pass with the available Apple Development identity. |
| 2026-07-31 | `scripts/verify-release-artifact.sh` against the local Release app | Expected strict failure: framework, Widget, helper binaries, feed, public key, signatures, Team ID, and source entitlements passed; Apple Development injected `com.apple.security.get-task-allow = true`, which the distribution verifier correctly rejects. |
| 2026-07-31 | `just test-ui` | Blocked before assertions: XCTest timed out enabling automation mode. Result: `Test-Curfew-2026.07.31_21-30-36--0700.xcresult`. |
| 2026-07-31 | Three consecutive full unit suites after final refactor | Pass; `290/290` tests passed with zero failures or skips in `/tmp/CurfewUnits-release-closure-final-{1,2,3}.xcresult`. |
| 2026-07-31 | `just test-ui` retry | Automation initialized and reached product assertions. Sparkle command passed; four semantic assertions exposed onboarding-copy, countdown-value, hold-control, and Settings-tab accessibility gaps. The later marketing capture lost the app PID and contaminated the active GUI test session, so this is red evidence rather than a passing gate. |
| 2026-07-31 | Debug build with `SWIFT_TREAT_WARNINGS_AS_ERRORS=YES SWIFT_SUPPRESS_WARNINGS=NO` | Pass after repairing Swift 6 actor isolation in App Intents, actor-bound dependency construction, the license callback, and MCP filesystem monitoring; obsolete CSV error handling was removed. |
| 2026-07-31 | Targeted `PersistentLockdownTests`, `LicenseGateTests`, and `MCPGatingTests` | Pass after actor-bound dependency construction and event-driven MCP monitor cleanup. |
| 2026-07-31 | Full `CurfewTests` suite and final `just check` | Pass; format lint, strict lint, unit tests, and Debug build all succeeded with zero SwiftLint violations. |
| 2026-07-31 | Final warning-as-error Debug build and `just build-release` | Pass; both Debug and Release app configurations compile after concurrency hardening. |
| 2026-07-31 | Embedded Plus public-key validation | Pass; active key is not the all-zero sentinel and decodes to exactly 32 bytes. Matching private-key/Worker proof remains pending external deployment evidence. |
| Pending | Signed Release archive and `scripts/verify-release-artifact.sh` | Pending Developer ID credentials. |

Local environment: macOS `26.5.1` (`25F80`), Xcode `26.6` (`17F113`). The
keychain exposes one valid Apple Development identity and no Developer ID
Application identity, so local success is not distribution evidence.

## Signed-device evidence

| Gate | Outcome | Evidence / failure |
|---|---|---|
| Gatekeeper, notarization, stapling, DMG install | Pending | Requires published signed artifact. |
| Widget gallery registration and App Group refresh | Pending | Requires signed installed app. |
| Helper install, approval, status, and reboot recovery | Pending | Requires sacrificial Mac. |
| Force-kill heartbeat timeout and one-shot shutdown | Pending | Destructive test; sacrificial Mac only. |
| Natural unlock and approved override | Pending | Requires installed helper. |
| In-app and shell uninstall | Pending | Requires installed helper. |
| Apple Events shutdown granted and denied | Pending | Requires signed app and controlled shutdown test. |
| Pro license activation and removal | Pending | Requires known-good production license. |
| MCP `curfew.status` | Pending | Requires installed candidate. |
| Five-minute warning-to-lockout flow | Pending | Requires installed candidate. |
| Sparkle `0.0.99` to `0.1.0` staging upgrade | Pending | Requires signed staging feed and candidates. |

## Failures

- 2026-07-31: macOS XCTest automation mode could not be enabled in three
  attempts, including the final `just test-ui` run. Zero UI assertions executed;
  this is an environment blocker, not a waived test.
- 2026-07-31: a later retry cleared the automation-mode blocker and reached UI
  assertions. Product accessibility gaps were repaired, but final verification
  was intentionally stopped because macOS XCUITest and full-screen capture use
  the operator's active desktop. Rerun on a dedicated GUI test host.
- 2026-07-31: strict artifact verification rejected the Apple Development build
  because it carries `get-task-allow`. The verifier was not weakened; rerun it
  against the Developer ID candidate.
